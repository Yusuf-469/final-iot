"const express = require('express'); const app = express(); app.use(express.json()); app.post('/test', (req, res) => { console.log('Body:', req.body); res.json({received: req.body}); }); app.listen(3003);" 
